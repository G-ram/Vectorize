#ifndef __SRM_H__
#define __SRM_H__

#include <thrust/host_vector.h>
#include <thrust/device_vector.h>
#include "struct.h"

class SRM{
	thrust:device_vector<edge> edges;
	public:
		SRM(thrust::device_vector<region>,
	private:
				
		bool mergeTest(region A, region B, thrust:host_vector regions);
}
void srm2(thrust::host_vector<region> regions, thrust:host_vector<edge> edges);

__host__ __device__ int max3(int x, int y, int z);

#endif

